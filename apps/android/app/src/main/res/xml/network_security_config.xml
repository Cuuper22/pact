<?xml version="1.0" encoding="utf-8"?>
<!--
  Production relays use HTTPS/WSS. For local development the default relay is
  http://10.0.2.2:8787 (the emulator's view of the host), so cleartext is
  permitted ONLY to loopback/dev hosts. Everything else stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
